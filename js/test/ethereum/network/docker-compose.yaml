version: '3'

services:


  proofServer1:
    image: appliedblockchain/parity-solo-instant:latest
    cmd: 
    restart: on-failure
    volumes:
      - /tmp/k0keys/
    ports:
      - 4000:4000
    networks:
      - main
  # - /tmp/k0keys:/tmp/k0keys:ro
  parity:
    image: appliedblockchain/parity-solo-instant:latest
    entrypoint: ["/parity/parity", "--config", "/solo/node.toml", "--unsafe-expose", "--min-gas-price", "0"]
    restart: always
    volumes:
      - parity:/solo/chains
    ports:
      - 8545:8545
      - 8546:8546
    networks:
      - main
volumes:
  # keys:
networks:
  main:

    